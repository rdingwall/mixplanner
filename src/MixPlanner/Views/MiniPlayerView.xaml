<UserControl x:Class="MixPlanner.Views.MiniPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:MixPlanner.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:MiniPlayerViewModel}">
    <StackPanel Orientation="Horizontal" 
                HorizontalAlignment="Right" 
                Visibility="{Binding HasTrackLoaded, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Button Content="{Binding PlayOrPauseButtonLabel}"
                Command="{Binding PlayOrPauseCommand}"/>
        <Label Content="{Binding Title}"/>
    </StackPanel>
</UserControl>
