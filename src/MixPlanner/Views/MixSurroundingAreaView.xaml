<UserControl x:Class="MixPlanner.Views.MixSurroundingAreaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mp="clr-namespace:MixPlanner.ViewModels"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             xmlns:views="clr-namespace:MixPlanner.Views"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance mp:MixSurroundingAreaViewModel}">
    <Grid>
        <GroupBox Header="Mix"
                  dd:DragDrop.IsDropTarget="True"
                  dd:DragDrop.DropHandler="{Binding}"
                  Background="Transparent">
            <views:MixView DataContext="{Binding Mix}"
                           Width="530"
                           
                           VerticalAlignment="Stretch"/>
        </GroupBox>
    </Grid>
</UserControl>
